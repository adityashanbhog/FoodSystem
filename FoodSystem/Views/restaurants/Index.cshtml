@model IEnumerable<FoodSystem.Models.restaurant>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.manager.manager_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.restaurant_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.restaurant_Address)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.manager.manager_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.restaurant_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.restaurant_Address)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.restaurantID }) |
            @Html.ActionLink("Details", "Details", new { id=item.restaurantID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.restaurantID })
        </td>
    </tr>
}

</table>
<br>
@Html.ActionLink("Home", "Index","adminhomepage")
<br>